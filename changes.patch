diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
new file mode 100644
index 0000000..34c4641
--- /dev/null
+++ b/.github/workflows/ci.yml
@@ -0,0 +1,32 @@
+name: CI
+
+on:
+  push:
+    branches: ['**']
+  pull_request:
+
+jobs:
+  verify:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v4
+      - uses: actions/setup-node@v4
+        with:
+          node-version: 22
+          cache: npm
+      - name: Install
+        run: npm ci --ignore-scripts
+      - name: Format Check
+        run: npm run format:check
+      - name: Lint
+        run: npm run lint
+      - name: Test (CI)
+        run: npm run test:ci
+      - name: i18n Extract
+        run: npm run i18n:extract
+      - name: Build
+        run: npm run build
+      - name: Build Localized
+        run: npm run build:localize
+      - name: Accessibility (CI)
+        run: npm run a11y:ci
diff --git a/pa11yci.json b/pa11yci.json
new file mode 100644
index 0000000..96e8bde
--- /dev/null
+++ b/pa11yci.json
@@ -0,0 +1,13 @@
+{
+  "defaults": {
+    "standard": "WCAG2AA",
+    "timeout": 30000,
+    "wait": 1000
+  },
+  "urls": [
+    "http://127.0.0.1:4173/catalog",
+    "http://127.0.0.1:4173/basket",
+    "http://127.0.0.1:4173/checkout",
+    "http://127.0.0.1:4173/account/login"
+  ]
+}
diff --git a/package.json b/package.json
index 3001cd2..3fae789 100644
--- a/package.json
+++ b/package.json
@@ -7,10 +7,14 @@
     "start": "npm run serve",
     "serve": "ng serve --host 0.0.0.0 --port 4200 --poll 2000",
     "build": "ng build",
+    "build:localize": "ng build --configuration production --localize",
+    "serve:a11y": "ng serve --host 127.0.0.1 --port 4173 --poll 2000",
     "watch": "ng build --watch --configuration development",
     "test": "ng test",
     "test:ci": "ng test --watch=false",
     "lint": "ng lint",
+    "i18n:extract": "ng extract-i18n --format=xlf2 --output-path src/locale --out-file messages.xlf",
+    "a11y:ci": "start-server-and-test \"npm run serve:a11y\" http://127.0.0.1:4173/catalog \"pa11y-ci --config pa11yci.json\"",
     "format": "prettier --write \"src/**/*.{ts,html,css}\" package.json eslint.config.js tsconfig*.json",
     "format:check": "prettier --check \"src/**/*.{ts,html,css}\" package.json eslint.config.js tsconfig*.json"
   },
@@ -29,24 +33,31 @@
   "private": true,
   "packageManager": "npm@10.9.2",
   "dependencies": {
-    "@angular/animations": "^21.2.0",
-    "@angular/common": "^21.0.0",
-    "@angular/compiler": "^21.0.0",
-    "@angular/core": "^21.0.0",
-    "@angular/forms": "^21.0.0",
-    "@angular/platform-browser": "^21.0.0",
-    "@angular/router": "^21.0.0",
+    "@angular/animations": "21.2.0",
+    "@angular/cdk": "21.2.0",
+    "@angular/common": "21.2.0",
+    "@angular/compiler": "21.2.0",
+    "@angular/core": "21.2.0",
+    "@angular/forms": "21.2.0",
+    "@angular/localize": "21.2.0",
+    "@angular/platform-browser": "21.2.0",
+    "@angular/platform-server": "21.2.0",
+    "@angular/router": "21.2.0",
+    "@angular/ssr": "21.2.0",
     "rxjs": "~7.8.0",
     "tslib": "^2.3.0"
   },
   "devDependencies": {
-    "@angular/build": "^21.0.0",
-    "@angular/cli": "^21.0.0",
-    "@angular/compiler-cli": "^21.0.0",
+    "@angular/build": "21.2.0",
+    "@angular/cli": "21.2.0",
+    "@angular/compiler-cli": "21.2.0",
+    "@types/node": "24.5.2",
     "angular-eslint": "21.2.0",
     "eslint": "^9.39.2",
     "jsdom": "^27.1.0",
+    "pa11y-ci": "^4.0.1",
     "prettier": "^3.6.2",
+    "start-server-and-test": "^2.1.3",
     "typescript": "~5.9.2",
     "typescript-eslint": "8.50.1",
     "vitest": "^4.0.8"
diff --git a/src/app/app.config.server.ts b/src/app/app.config.server.ts
new file mode 100644
index 0000000..ac3cf2b
--- /dev/null
+++ b/src/app/app.config.server.ts
@@ -0,0 +1,11 @@
+import { ApplicationConfig, mergeApplicationConfig } from '@angular/core';
+import { provideClientHydration } from '@angular/platform-browser';
+import { provideServerRendering, withRoutes } from '@angular/ssr';
+import { appConfig } from './app.config';
+import { serverRoutes } from './app.routes.server';
+
+const serverConfig: ApplicationConfig = {
+  providers: [provideClientHydration(), provideServerRendering(withRoutes(serverRoutes))],
+};
+
+export const config = mergeApplicationConfig(appConfig, serverConfig);
diff --git a/src/app/features/account/login-page.component.html b/src/app/features/account/login-page.component.html
index f8ecb45..911ba83 100644
--- a/src/app/features/account/login-page.component.html
+++ b/src/app/features/account/login-page.component.html
@@ -3,7 +3,7 @@
 
   <form [formGroup]="loginForm" (ngSubmit)="signIn()" novalidate>
     <label for="email">Email</label>
-    <input id="email" type="email" formControlName="email" autocomplete="username" />
+    <input id="email" type="email" formControlName="email" autocomplete="email" />
     @if (loginForm.controls.email.touched && loginForm.controls.email.invalid) {
       <p>Email is required and must be valid.</p>
     }
diff --git a/src/app/features/checkout/checkout-page.component.html b/src/app/features/checkout/checkout-page.component.html
index ffe1a64..c6028aa 100644
--- a/src/app/features/checkout/checkout-page.component.html
+++ b/src/app/features/checkout/checkout-page.component.html
@@ -2,7 +2,7 @@
   <app-page-title title="Checkout" subtitle="Review details before payment" />
 
   @if (hasSummary()) {
-    <section class="form-summary" aria-live="polite">
+    <section class="form-summary" aria-live="polite" role="alert" aria-atomic="true">
       <h2>Form summary</h2>
       <ul>
         @for (message of formSummaryErrors(); track message) {
@@ -249,7 +249,7 @@
           <label for="cvc">CVC</label>
           <input
             id="cvc"
-            type="password"
+            type="text"
             inputmode="numeric"
             autocomplete="cc-csc"
             formControlName="cvc"
@@ -371,7 +371,7 @@
     <p class="success-message" aria-live="polite">{{ message }}</p>
   }
   @if (paymentErrorMessage(); as paymentError) {
-    <p class="field-error" aria-live="polite">{{ paymentError }}</p>
+    <p class="field-error" aria-live="assertive" role="alert">{{ paymentError }}</p>
   }
 
   <p>Unsaved changes guard is active on this route.</p>
diff --git a/src/app/features/shell/shell-layout.component.css b/src/app/features/shell/shell-layout.component.css
index d102a58..8f7194d 100644
--- a/src/app/features/shell/shell-layout.component.css
+++ b/src/app/features/shell/shell-layout.component.css
@@ -11,6 +11,27 @@
   background: #f5f7fa;
 }
 
+.skip-link {
+  position: absolute;
+  left: 0.75rem;
+  top: 0.5rem;
+  transform: translateY(-200%);
+  background: #0f1728;
+  color: #fff;
+  padding: 0.5rem 0.75rem;
+  border-radius: 0.35rem;
+  z-index: 999;
+}
+
+.skip-link:focus {
+  transform: translateY(0);
+}
+
+.skip-link:focus-visible {
+  outline: 2px solid #38bdf8;
+  outline-offset: 2px;
+}
+
 .shell-header {
   display: flex;
   align-items: center;
diff --git a/src/app/features/shell/shell-layout.component.html b/src/app/features/shell/shell-layout.component.html
index 0845c35..891b4b2 100644
--- a/src/app/features/shell/shell-layout.component.html
+++ b/src/app/features/shell/shell-layout.component.html
@@ -1,4 +1,7 @@
 <div class="shell">
+  <a class="skip-link" href="#main-content" aria-label="Skip to main content">
+    Skip to main content
+  </a>
   <header class="shell-header">
     <a class="brand" routerLink="/catalog">Workshop Booking</a>
 
@@ -21,7 +24,7 @@
     <div class="route-loading" aria-live="polite">Navigating...</div>
   }
 
-  <main class="shell-content" [@routeTransition]="routeKey(outlet)">
+  <main id="main-content" class="shell-content" tabindex="-1" [@routeTransition]="routeKey(outlet)">
     <router-outlet #outlet="outlet" />
   </main>
 
